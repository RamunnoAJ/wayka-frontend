import { Ajustes } from '../../../src/features/tutor-movil';
import { EntradaDePantalla } from '../../../src/components';

/** Ficha propia, avisos y cuenta (Alcance de Plataformas, 5.8). */
export default function AjustesDelTutor() {
  return (
    <EntradaDePantalla>
      <Ajustes />
    </EntradaDePantalla>
  );
}
