Boton de accion; UNA sola accion primaria (violeta) por pantalla. 'accent' esta deprecado (alias de primary).

```jsx
<Button variant="primary" iconLeft="plus">Nuevo evento</Button>
```

variant: primary/secondary/ghost/danger · size: sm/md/lg/touch (52px en movil) · block · loading (spinner + disabled)
