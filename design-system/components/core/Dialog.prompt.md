Modal centrado para confirmaciones y formularios cortos.

```jsx
<Dialog open title="Finalizar tratamiento" footer={<Button>Confirmar</Button>} onClose={...}>…</Dialog>
```

width por defecto 520; el pie tiene fondo sunken.
