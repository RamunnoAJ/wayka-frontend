Contenedor base: fondo blanco, borde 1px, radio 16, sombra baja. La jerarquia entre cards se resuelve con contenido y espaciado, no con fondos de color.

```jsx
<Card title="Historial" action={<Button variant="ghost" size="sm">Ver todo</Button>}>…</Card>
```

tone: default/sunken ('clinical' y 'owner' deprecados: renderizan como default; la autoria la marca DataField) · padded={false} para listas a sangre.
