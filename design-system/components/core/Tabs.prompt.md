Navegacion de seccion (`underline`) o filtro rapido (`pill`).

```jsx
<Tabs items={[{value:"hoy",label:"Hoy",count:8}]} value={v} onChange={setV} />
```

count muestra un contador gris a la derecha del label.
