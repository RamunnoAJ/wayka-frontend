Fila de dato que declara autoria (clinico vs tutor) y si es editable.

```jsx
<DataField label="Peso" value="8,4" unit="kg" source="owner" editable onEdit={...} />
```

No editable muestra candado "Solo lectura": clave en la app del tutor.
